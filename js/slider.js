// Sliders 

const infoSlider = new Swiper('.info__slider', {
  slidesPerView: "auto",
  spaceBetween: 30,
  freeMode: true,
});

const newsSlider = new Swiper('.news__slider', {
  slidesPerView: "auto",
  spaceBetween: 30,
  freeMode: true,
});